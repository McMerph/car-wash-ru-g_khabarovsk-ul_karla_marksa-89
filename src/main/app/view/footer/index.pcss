@import '../style/root.pcss';

.footer {
  background-color: #363636;
  margin: 0 -5px;
}

.footer-content {
  font: normal 300 18px/2 var(--open-sans-font-stack);
  display: flex;
  align-items: center;
  justify-content: space-around;
  padding: 43px 0;
}

.footer-content_company,
.footer-content_nav,
.footer-content_account {
  display: flex;
  align-items: center;
}

.footer-content_company_logo {
  flex: 0 0 52px;
  margin-right: 8px;
}

.footer-content_company_name {
  color: #a8a8a8;
  font: normal 300 18px/2 var(--proxima-nova-font-stack);
}

.footer-content_bar {
  height: 72px;
  border-left: 1px solid #626262;
}

.footer-content_nav_a {
  @apply --underline-decoration;

  color: #adadad;
  margin: 0 5px;

  &::before {
    bottom: 7px;
  }

  &:hover {
    color: #0056b3;
  }
}

.footer-content_account_a {
  @apply --underline-decoration;

  color: #efeab1;

  &::before {
    bottom: 6px;
    border-bottom: 1px dashed;
  }

  &:hover {
    color: #0056b3;
  }
}

.footer-content_account_logo {
  flex: 0 0 24px;
  margin-right: 20px;
}

@media (max-width: 730px) {
  .footer-content_nav {
    flex-direction: column;
  }
}

@media (max-width: 590px) {
  .footer-content_company {
    flex-direction: column;
  }
}

@media (max-width: 530px) {
  .footer-content {
    flex-direction: column;
  }

  .footer-content_company {
    flex-direction: row;
  }

  .footer-content_nav {
    flex-direction: row;
    margin: 15px 0;
  }

  .footer-content_bar {
    display: none;
  }
}
