@import 'root.pcss';

html {
  background: var(--background-color);
  touch-action: manipulation;
}

a {
  cursor: pointer;

  &:hover {
    color: var(--hover-color);
  }
}

button {
  cursor: pointer;
}
