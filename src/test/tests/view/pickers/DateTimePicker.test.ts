test('disabledDates generation', () => {
    // TODO Fix
    // const api: Api = new MockApi();
    // const availability: Availability = api.retrieveAvailability();
    // const availabilityHandler: AvailabilityHandler = new AvailabilityHandler(availability);
    // const dateTimePicker: DateTimePicker = new DateTimePicker({
    //     availableTimes: availabilityHandler.getCheckInTimes(),
    //     initialMonth: new Date(),
    //     disabledTimeStamps: availabilityHandler.getDisabledTimeStamps()
    // });
    // dateTimePicker.isPicked();
    // dateTimePicker.disa

    // expect(noServiceDates.length).toBe(1);
    // expect(DateUtils.equalsDateOfMonth(noServiceDates[0], new Date())).toBeTruthy();
});
